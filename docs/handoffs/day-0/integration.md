# Day 0 Integration Handoff

- Lane and branch: integration / `integration/day0`
- Base: `day0-prepared` (`eedde4b`)
- Head: `5ae3ce3` (merged Wave 0A application candidate; integration updates
  remain uncommitted by instruction)
- Gate: Wave 0A accepted; Wave 0B is now in progress

## Changed paths

- `Makefile`: the shared pytest harness now exposes every Day 0 `src/` package,
  includes the integration foundation gate in `verify-wave-0a`, and makes
  `verify-wave-0b` require preflight and architecture checks.
- `tests/integration/test_wave0a_foundation.py`: verifies all package imports,
  six seed products, deterministic synthetic connectors, four agent roles,
  FastAPI ASGI health, and disabled external providers.
- `apps/portfolio-risk-workbench/servicefabric-package.json`: manifest hashes
  were deterministically synchronized and checked.
- `config/agent/day0/status.json`, `docs/workplans/current.md`, and
  `docs/workplans/day-0/wave-0b.md`: advance the wave state and define the
  bounded Wave 0B plan.

## Evidence and validation

- All merged lane handoffs were inspected: domain, planning, data, agents,
  application, and the preceding integration handoff.
- `make test-integration` — PASS (`8 passed`).
- `make test-architecture` — PASS (`8 passed`).
- `make test-application` — the focused route and manifest checks pass, but
  the canonical-host test fails because the clean pinned upstream host accepts
  only its reviewed AP-01A package.
- `scripts/day0/update_manifest_hashes.py apps/portfolio-risk-workbench/servicefabric-package.json --check` — PASS.
- `git diff --check` — PASS.
- `make verify-wave-0a` — PASS (`8` architecture tests; `8` integration
  tests).

## Deviations, blockers, and limitations

- `make preflight` remains blocked at `env-check` by expired local GitHub CLI
  authentication. This is an environment-only blocker; repository checks and
  the Wave 0A gate do not require network access.
- The application handoff described a historical upstream-host exception, but
  the checked merged worktree has a clean pinned `vendor/servicefabric`
  submodule. Its canonical-host test therefore rejects the workbench as an
  unapproved AP-01A package. Upstream approval is required; no vendor change
  was accepted.
- The pre-existing unstaged application-manifest formatting change was
  preserved. Its declared hashes are current; it was not overwritten.
- Wave 0A remains a local synthetic foundation: no provider, broker, trading,
  external LLM, or investment-advice behavior is enabled.

## Rollback

Revert the focused integration changes, restore Wave 0A as current with Wave
0B queued in `config/agent/day0/status.json`, restore the Wave 0A workplan
pointer, and retain the ServiceFabric pin. The manifest can be regenerated by
the deterministic hash updater.

## Recommended next action

Begin only the scoped Wave 0B candidate work in the defined lane order.
