# Day 1 Wave 1A integration handoff

- Lane and branch: integration / `integration/day1`
- Base: `day1-prepared` (`01ff31a3daa0db815da51da16ca19099005149e7`)
- Head: `HEAD`; the Wave 1A gate correction is a focused candidate atop
  `003010c`

## Changed paths

Day 1 status/current-workplan records, Day 1 dependency input and hash lock,
Day 1 bootstrap, lifecycle and lane-path checkers, current-gate verifier,
Makefile targets, lifecycle workflow, Day 1 prompt templates, architecture
tests, and this handoff.

Lane-path validation is applied to each candidate using its originating lane
and exact base/head range. Cumulative integration and QA gates validate the
control-plane manifests and must not reinterpret all accepted specialist paths
as integration-lane ownership.

## Evidence and tests

The status now activates `D1-WAVE-1A` with preparation complete and preserves
the `day0-complete` base tag. The dependency lock was generated by
`pip-compile --generate-hashes` from `requirements/day1.in`; it inherits Day 0
and adds only Jinja2 and python-multipart. A deleted/fresh Day 1 virtual
environment installed the lock with `--require-hashes` and passed `pip check`.

Executed: `make preflight`, fresh
`DAY1_VENV=/tmp/day1-validation-venv make day1-env`,
`DAY1_VENV=/tmp/day1-validation-venv make test-day1-architecture`, and
`DAY1_VENV=/tmp/day1-validation-venv make verify-day1-current`, `make day1-env`,
`make verify-day1-current`, `make verify-day0`,
`python3.11 scripts/day1/check_lane_paths.py --lane integration --base
day1-prepared --head HEAD --manifest config/agent/day1/lanes.json`,
`python3.11 -m py_compile scripts/day1/check_preparation.py
scripts/day1/check_lane_paths.py scripts/day1/verify_current.py`, and
`git diff --check`. The focused Day 1 architecture suite passed 25 tests after
review remediation.

Review remediation added lifecycle-derived current-workplan validation, full
status validation before gate selection, and regression coverage preventing a
cumulative integration-lane check from rejecting accepted specialist changes.

### Environment-path correction (2026-07-22)

An empty `DAY1_VENV` previously produced two conflicting interpretations:
the bootstrap script created the repository-local `.venv-day1`, while Make
looked for `/bin/python`. The Makefile now normalizes an empty environment or
command-line value to `$(CURDIR)/.venv-day1` before deriving `DAY1_PYTHON`.
Non-empty overrides remain unchanged. The focused candidate changes
`Makefile`, `tests/architecture/test_day1_preparation.py`, and this handoff.

Executed for the correction: `make preflight`, `DAY1_VENV= make
test-day1-architecture` (27 passed), `DAY1_VENV= make verify-day1-current`,
and `git diff --check`. The current-wave verifier passed preparation and
correctly reported Wave 1A as still in progress.

### Wave 1A environment-boundary correction (2026-07-22)

The cumulative Wave 1A target inherited `verify-day0`, which executes every
application-importing suite with the Day 0 environment. The Day 1 Workbench
correctly depends on Jinja2, a Day 1-only locked dependency, so that path
failed before test execution. Wave 1A now retains the Day 0 architecture,
domain, planning, data, capability, and agent suites, while running the
Workbench, integration, and journey suites with the Day 1 environment.

Executed: `make test-day1-architecture` (28 passed), `make verify-wave-1a`
(PASS), and `git diff --check`. The full gate included 42 application, 10
integration, 2 journey, 22 planning, 6 research, and 28 Day 1 architecture
tests, in addition to the retained Day 0 suites.

## Deviations, blockers, limitations

No product feature, provider, LLM, SQL execution, notebook, broker, order,
trade, rebalancing, or ServiceFabric submodule change is included. Wave 1A is
in progress, so `verify-day1-current` intentionally does not report its gate
as complete. The separate process-host smoke target remains local-only.

## Rollback and next action

Revert this integration candidate back to `day1-prepared`; leave Day 0 records
and `vendor/servicefabric` untouched. Next, accept only focused Wave 1A lane
candidate changes after the lane-path checker and the appropriate completed
wave gate pass.
